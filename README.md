PLEASE READ ME:

**Instructions:**
1: Install the Dependancys by using wally install in the terminal.
2: Run rojo serve in the Terminal. (Since it already contains an aftman file I'm pretty sure running Rojo without terminal wont work.)
3: Open a Blank Workspace in roblox Connect to your running Rojo. (Assets are added in Rojo Files so they load on Connect)

**Using GIT:**
1: Always Pull the latest changes. git checkout main, git pull origin main
2: Create a Feture Branch. git checkout -b feature/example-of-new-thing
3: Make sure you always commit changes. git status, git add , git commit -m "New Change Comment"
4: Push and Open a Pull request. git push origin feature/example-of-new-thing

Required:
1: **Visual Studio Code**
2: **Rojo CLI & Wally CLI**
3: **Roblox Studio**

### 1. Clone the Repository
Clone this repository to your local machine and navigate into the folder:
```bash
git clone <REPOS-URL>
cd ZNB 
